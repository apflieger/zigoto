{% form_theme form 'bootstrap_3_layout.html.twig' %}
<div class="container">
    <div class="row">
        <div class="col-sm-6 col-sm-offset-3">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
</div>